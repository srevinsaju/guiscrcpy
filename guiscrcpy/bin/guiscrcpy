#!/usr/bin/env python3
import os
import platform
import sys
import subprocess
print("RUNNING FROM PIP :: 0")
"""
if platform.system()=="Windows":
	os.system("python -c 'import guiscrcpy; guiscrcpy.runnow()'")
else:
	os.system("python3 -c 'import guiscrcpy; guiscrcpy.runnow()'")
    """
print(__file__)
if(platform.system()=="Windows"):
            print("DETECTED OS :: WINDOWS")
            pythonexec = "python"
else:
            print("DETECTED OS :: *NIX")
            pythonexec = "python3"
print(os.getcwd())
filename = str(__file__)[:-(len("bin/guiscrcpy"))]
os.chdir(str(os.path.abspath(__file__)[:-len("bin/guiscrcpy")]))
arg1 = " "
print(os.getcwd())
print(sys.argv)
for i in sys.argv[1:]:
        arg1 += i
        
cmd = pythonexec + ' __init__.py ' + arg1
print(cmd)
tmp = subprocess.Popen(cmd, shell=True)

